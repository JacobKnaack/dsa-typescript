function longestPalindrome(s: string) {
  // calculate the number of even characters and odd characters

  // if we any odds, return the string length, minus the number of odds found, plus 1.
  
  // if not, return the length of the string.
}